#pragma once
/*
#include "Buffer.h"

class Device;
class Buffer;

class IndexBuffer
{
public:
    IndexBuffer(Device& device, const vector<uint16_t> indices);

public:
    Device& device;
    Buffer* buffer;
};
*/
